# Core dependencies
torch>=2.0.0,<2.3.0
torchaudio>=2.0.0,<2.3.0
librosa>=0.10.0
numpy>=1.23.0,<2.0.0
scipy>=1.10.0
numba==0.61.2                # prebuilt wheel; required by librosa on macOS x86_64
llvmlite==0.44.0

# ML and data processing
scikit-learn>=1.2.0
# scikit-audio>=0.1.0
matplotlib>=3.7.0
seaborn>=0.12.0
pandas>=1.5.0

# Configuration and utilities
pydantic>=2.0.0
pyyaml>=6.0
python-dotenv>=1.0.0
loguru>=0.7.0

# API and web frameworks
fastapi>=0.95.0
uvicorn>=0.21.0
pydantic[email]>=2.0.0

# Development and testing
pytest>=7.3.0
pytest-cov>=4.0.0
pytest-asyncio>=0.21.0
black>=23.3.0
isort>=5.12.0
flake8>=6.0.0
mypy>=1.0.0
pre-commit>=3.3.0

# Optional: Monitoring and experiment tracking
# wandb>=0.14.0
# mlflow>=2.2.0

# ── VoiceGuard Agent Layer ────────────────────────────────────────────────────
# LLM
openai>=1.30.0

# Memory — vector store
chromadb>=0.5.0
sentence-transformers>=3.0.0,<4.0.0
transformers>=4.41.0,<5.0.0   # transformers 5.x requires torch>=2.4 (unavailable on mac x86_64)

# Memory — structured (SQLite ORM)
sqlalchemy>=2.0.0

# API additions
python-multipart>=0.0.9      # multipart form upload for /agent/analyze
httpx>=0.27.0                # async HTTP client (used in tests)
